---
name: jadu-kar
description: Retired - merged into jadu-bidar. Use when the user invokes jadu-kar out of habit; redirect them to jadu-bidar, which now handles session start and task management together.
---

# Jadu Kar - Merged into Bidar

`jadu-kar` no longer exists as a separate skill. Session start and task management are now
one skill: `jadu-bidar`.

- Starting a session, seeing the live backlog table, adding/updating/reprioritizing tasks,
  and the implementation handoff (numbered plan -> `go`) all live in `jadu-bidar` now.
- If a project is already resolved this session, just describe what you want (add a task,
  mark something done, show the backlog) - `jadu-bidar` picks up from wherever you are.

Use `jadu-bidar` instead. This stub exists so old muscle memory does not hit a dead end.
